export const metadata = { title: "Data Selector — Clarity Components" };

export default function DataSelectorShowcase() {
  return (
    <div style={{ fontFamily: "var(--font-family)", background: "var(--surface-secondary)", color: "var(--content-primary)", minHeight: "100vh", padding: "48px 40px 80px" }}>
      <div style={{ maxWidth: 780, margin: "0 auto" }}>
        <h1 style={{ fontSize: 18, fontWeight: 500, marginBottom: 40 }}>Data Selector</h1>
        <p style={{ color: "var(--content-tertiary)", fontSize: 13 }}>Component showcase — view the full implementation in the original HTML files.</p>
      </div>
    </div>
  );
}
